export default class Producto {
    constructor(nombre, precio, description, image, code , stock, status ) {
        this.nombre = nombre;
        this.precio = precio;
        this.description = description
        this.image = image
        this.code = code
        this.stock = stock
        this.status = status
    }
}